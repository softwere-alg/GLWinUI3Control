<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GLWinUI3Control">

    <Style TargetType="local:GLWinUI3Control" >
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:GLWinUI3Control">
                    <Grid Background="{TemplateBinding Background}">
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="local:GLWinUI3ControlRL" >
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:GLWinUI3ControlRL">
                    <Grid Background="{TemplateBinding Background}">
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
